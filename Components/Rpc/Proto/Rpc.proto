syntax = "proto3";

package ADL.Rpc.Proto.Service;

message PingRequest {
    int32 ping = 1;
}

message PongResponse {
    int32 pong = 2;
}

service RpcService {
    rpc Greeting(PingRequest) returns (PongResponse);
}